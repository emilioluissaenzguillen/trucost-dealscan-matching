# Templates

This folder contains mock data and review-file templates that mirror the structure of important inputs in the matching workflow.

The values are synthetic placeholders, not real vendor data.

These files are included to make the pipeline easier to understand without redistributing licensed or project-specific data.
